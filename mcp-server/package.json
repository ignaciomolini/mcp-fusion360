{
  "name": "mcp-fusion360",
  "version": "0.1.0",
  "description": "MCP server for Fusion 360 CAD operations — translates MCP tool calls to JSON-RPC HTTP requests",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}